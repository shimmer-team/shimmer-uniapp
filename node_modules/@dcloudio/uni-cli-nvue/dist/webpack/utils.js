"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatErrors = void 0;
const uni_cli_shared_1 = require("@dcloudio/uni-cli-shared");
const preprocessors = ['compile-dart-sass', 'compile-less', 'compile-stylus'];
function formatErrors(errors) {
    return errors
        .filter(({ message }) => {
        if ((0, uni_cli_shared_1.isInHBuilderX)()) {
            const preprocessor = preprocessors.find((preprocessor) => message.includes(preprocessor));
            if (preprocessor) {
                (0, uni_cli_shared_1.installHBuilderXPlugin)(preprocessor);
                const langs = preprocessor.split('-');
                console.error((0, uni_cli_shared_1.formatInstallHBuilderXPluginTips)(langs[langs.length - 1], preprocessor));
                return false;
            }
        }
        return true;
    })
        .join('\n');
}
exports.formatErrors = formatErrors;
