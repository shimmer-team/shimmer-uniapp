export declare function createOneOf(preLoader?: {
    loader: string;
    options?: Record<string, any>;
}): ({
    resourceQuery: RegExp;
    use: {
        loader: string;
    }[];
} | {
    use: {
        loader: string;
    }[];
    resourceQuery?: undefined;
})[];
