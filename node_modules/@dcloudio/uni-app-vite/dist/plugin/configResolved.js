"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.configResolved = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const uni_cli_shared_1 = require("@dcloudio/uni-cli-shared");
let appCss = '';
function normalizeCssChunkFilename(id) {
    return ((0, uni_cli_shared_1.removeExt)((0, uni_cli_shared_1.normalizePath)(path_1.default.relative(process.env.UNI_INPUT_DIR, id))) +
        '.css');
}
const configResolved = (config) => {
    const inputDir = process.env.UNI_INPUT_DIR;
    const mainPath = (0, uni_cli_shared_1.resolveMainPathOnce)(inputDir);
    (0, uni_cli_shared_1.injectCssPlugin)(config);
    (0, uni_cli_shared_1.injectCssPostPlugin)(config, {
        chunkCssFilename(id) {
            if (id === mainPath) {
                return 'app.css';
            }
            else if ((0, uni_cli_shared_1.isUniPageSfcFile)(id, inputDir)) {
                return normalizeCssChunkFilename(id);
            }
        },
        chunkCssCode(filename, cssCode) {
            if (filename === 'app.css') {
                if (!appCss) {
                    appCss = fs_1.default.readFileSync(require.resolve('@dcloudio/uni-app-plus/dist/style.css'), 'utf8');
                }
                return appCss + '\n' + cssCode;
            }
            return cssCode;
        },
    });
    // 强制不inline
    config.build.assetsInlineLimit = 0;
    (0, uni_cli_shared_1.injectAssetPlugin)(config);
};
exports.configResolved = configResolved;
